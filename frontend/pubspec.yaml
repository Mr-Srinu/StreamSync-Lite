name: streamsync_lite
description: StreamSync Lite Flutter client
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  youtube_player_flutter: ^9.1.3
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  http: ^1.6.0
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
